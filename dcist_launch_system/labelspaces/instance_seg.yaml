---
label_names:
  - {label: 0, name: ignore}
  - {label: 1, name: chair}
  - {label: 2, name: cone}
  - {label: 3, name: bag}
  - {label: 4, name: table}
  - {label: 5, name: tree}
  - {label: 6, name: car}
  - {label: 7, name: van}
  - {label: 8, name: truck}
  - {label: 9, name: bus}
  - {label: 10, name: bicycle}
  - {label: 11, name: door}
  - {label: 12, name: pole}
  - {label: 13, name: fence}
  - {label: 14, name: sign}
  - {label: 15, name: window}
  - {label: 16, name: bed}
  - {label: 17, name: box}
  - {label: 18, name: basket}
  - {label: 19, name: seating}
  - {label: 20, name: flag}
  - {label: 21, name: light}
  - {label: 22, name: trash}
  - {label: 23, name: clothes}
  - {label: 24, name: ball}
  # Warehouse-prop classes (isaac_sim mapping harness, 2026-07-18). Indices
  # must match experiment_overrides/isaac_sim/instance_seg_overlay.yaml's
  # positional text_prompt ("fire extinguisher" prompt -> fire_extinguisher
  # name per the no-spaces convention). Only the isaac_sim experiment's
  # instance_seg config emits these labels; other experiments never produce
  # ids > 24, so appending here is backward-compatible.
  - {label: 25, name: pallet}
  - {label: 26, name: forklift}
  - {label: 27, name: shelf}
  - {label: 28, name: barrel}
  - {label: 29, name: ladder}
  - {label: 30, name: fire_extinguisher}

  # Environment classes for the realistic indoor/outdoor environments
  # (2026-07-27). floor/wall come from the scan-derived and campus_a
  # environments; terrain/tent/barrier/sandbag/vehicle/tree from the Buckner
  # DEM environment. Appended, so existing ids are untouched.
  - {label: 31, name: floor}
  - {label: 32, name: wall}
  - {label: 33, name: bench}
  - {label: 34, name: locker}
  - {label: 35, name: terrain}
  - {label: 36, name: tent}
  - {label: 37, name: barrier}
  - {label: 38, name: sandbag}
  - {label: 39, name: vehicle}
  - {label: 40, name: tree}

  # Exploration-mission prop classes (2026-07-28), curated free-standing
  # object list for catalog-driven scenario spawning
  # (dcist_sim/scenarios/exploration/prop_classes.yaml, Task 3). 6 of its 19
  # curated names already exist in this labelspace at lower ids -- chair(1),
  # table(4), box(17), shelf(27), fire_extinguisher(30), bench(33) -- and
  # class_to_labelspace_id() already resolves them correctly for exploration
  # spawns, so they are NOT re-registered here (unlike the `tree` 5-vs-40
  # duplicate above, these names have locked-in test expectations in
  # test_gt_semantics.py and are used by other isaac_sim scenarios via the
  # SAME shared LABELSPACE_NAME_TO_ID dict; shadowing them with new ids would
  # silently redirect those other scenarios' GT semantics). Only the 13
  # genuinely new prop names are appended below, alphabetically, ids 41+.
  - {label: 41, name: backpack}
  - {label: 42, name: cabinet}
  - {label: 43, name: cart}
  - {label: 44, name: desk}
  - {label: 45, name: display_case}
  - {label: 46, name: monitor}
  - {label: 47, name: plant}
  - {label: 48, name: printer}
  - {label: 49, name: recycling_bin}
  - {label: 50, name: refrigerator}
  - {label: 51, name: stool}
  - {label: 52, name: trash_can}
  - {label: 53, name: water_bottle}
